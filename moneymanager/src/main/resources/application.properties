#application properties
# MySQl
spring.datasource.url = jdbc:mysql://localhost:3306/moneymanager
spring.datasource.username = root
spring.datasource.password = Yrpatni@2223
server.servlet.context-path = /api/v1.0

#spring.profiles.active = prod
# this will load application-prod.properties and will override values from this file

# JPA
spring.jpa.hibernate.ddl-auto = update
spring.jpa.show-sql = true
spring.jpa.properties.hibernate.format_sql = true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

#Email Configuration
spring.mail.host =smtp-relay.brevo.com
spring.mail.port = 587
spring.mail.username=${BREVO_USERNAME}
spring.mail.password =${BREVO_PASSWORD}
spring.mail.properties.mail.smtp.auth = true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.from = ${BREVO_EMAIL}


# JWT Configuration
jwt.secret=aVeryLongAndSecureSecretKeyThatIs256BitsLongForHS256Algorithm1234567890
jwt.expiration=36000000

money.manager.frontend.url = http://localhost:5173

app.activation.url = ${MONEY_MANAGER_BACKEND_URL}


server.port=${PORT:9090}